{
  "name": "demo-hh-packages",
  "devDependencies": {
    "tsup": "^8.1.0"
  },
  "scripts": {
    "deploy": "hardhat ignition deploy ignition/modules/Lock.ts",
    "build": "tsup cli.ts deploy.ts --format esm "
  },
  "version": "0.0.22",
  "bin": {
    "demo": "dist/cli.mjs",
    "deploy": "dist/deploy.mjs"
  },
  "dependencies": {
    "@nomicfoundation/hardhat-toolbox-viem": "^3.0.0",
    "hardhat": "^2.22.5",
    "viem": "^2.17.3"
  }
}
